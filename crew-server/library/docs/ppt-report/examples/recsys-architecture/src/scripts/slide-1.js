// 推荐系统分层架构图 —— 全静态，无需任何 JS。
// （旧版这里有 setColumns / equalize 测量重排，已随模板一起改为全静态 CSS 移除：
//  三 tier 用 grid 1fr×3 等高封顶、chip 用 flex-wrap + align-items:center 居中，
//  布局确定性一次成，不再依赖 JS 测量。）
function initSlide1() {}
