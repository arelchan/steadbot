# EverBot · 产品形态上的几个决定

这份文档记录 EverBot 在「下一代 agent 产品长什么样」上做过的、和主流做法不同的决定。每条写四件事：主流怎么做、我们怎么做、为什么、落在代码哪里。

维护规则：新的形态决定落地时加一条；改了主意就改原文，不留「曾经」。实现细节不写在这里，写在各包的 README 和代码注释里。

---

## 1. 只有新 bot，没有新会话

**主流**：左栏是一串会话，每次开新对话从零开始，上下文靠用户复述或靠一个全局 memory 补。

**我们**：没有「新建会话」这个动作。左栏是一个个 bot，每个 bot 只有一条连续的对话，从它出生那天一直往下写。要换个话题就找另一个 bot，或者新建一个 bot。「清空聊天记录」存在，但它是和这个人「断交重来」，不是日常操作。

**为什么**：agent 的价值在于它记得你、记得手上的事。会话一开新，这段关系就归零了。一条连续对话逼着 bot 的记忆、事项、人设都长在这条线上，也逼着我们把「上下文太长」当成运行时该解决的问题（压缩、事项、记忆），而不是甩给用户。

**在哪**：`crew-server/src/types.ts` 的 `ThreadId` 只有 `bot:<id>` 和 `matter:<id>` 两种；pi 的会话文件按 bot 一份长期保留（`bots.ts` `ensure()`）。

## 2. 交互以异步委托为主，一切先落成事项

**主流**：同步问答，用户盯着流式输出等结果。

**我们**：用户说一句话就走。bot 收到每条消息先判断是新建、更新还是关闭哪个事项，落了再回；干活在后台，进度写在事项上（`open / doing / waiting / blocked / done`），要用户拍板时发一张卡（确认 / 选择 / 卡住），用户什么时候有空什么时候点。事项面板常驻在对话右侧，不需要 bot 播报。

**为什么**：值得交给 agent 的事大多不是三秒能答完的。产品要为「交代完去干别的」设计，而不是为「看着它打字」设计。事项是委托的凭证：用户不用记得自己交代过什么，bot 也不用靠聊天记录翻自己干到哪了。

**在哪**：`crew-server/src/extensions/todo.ts`、`ask.ts`、`act.ts`；`identity.ts` 的「工作方式」第一条；前端 `RightPanel.tsx`。

## 3. 找不找用户，是 bot 自己的判断

**主流**：系统级的通知设置：摘要频率、免打扰档位、按类型开关。

**我们**：没有这些。什么值得打断用户，由 bot 按它对这个用户的了解判断；用户嫌吵或嫌它不吭声，直接跟它说，它用 `build` 把标准写进自己的工作方式。系统只提供一个总开关和每个群的开关。

**为什么**：打扰阈值是关系的一部分，不是设置项。摘要和档位是把「bot 不会判断」这个缺陷制度化。我们赌 bot 能学会，并把纠正的成本压到一句话。

**在哪**：`identity.ts` 的 `REACH_OUT_RULE`；`notifier.ts` 只做「bot 说了话 → 要不要弹一下」，没有攒批。

## 4. 随时插话，bot 立刻改主意

**主流**：要么生成中不能发消息，要么发了排队，等这轮说完才看到。

**我们**：bot 正在说话时发消息，它立刻停，半句留在屏幕上标「被打断」，下一轮先看新消息重新判断刚才在做的哪些还成立，不补完旧方案、不重复。bot 正在跑工具时发消息，正在跑的那个让它跑完（写到一半杀掉会坏文件），同一批里还没开始的作废，新消息在下一次调模型前送到。

另外，短时间内连发的几条消息合成一轮处理（1.2 秒窗口、最长 3.5 秒），用户可以像在 IM 里一样分几条说完一件事。

**为什么**：改主意是委托里最常见的事。「等它说完旧方案再纠正」浪费的不只是时间，还有用户对 bot 的信任。技术上 pi 会把被中断的输出从模型上下文里整条丢掉，所以我们把半句引用回给模型，明确说在哪被打断。

**在哪**：`crew-server/src/bots.ts` `send()` 的 `busyHere` 分支、`cutInPrompt()`、`tool_call` 拦截钩子；`GATHER_MS`。

## 5. 一句话生出 bot，bot 自己长大

**主流**：建 agent 要填表：名字、system prompt、工具勾选、模型参数。

**我们**：在输入框说一句要办的事，bot 就出生了：名字、简介、职责、人设、头像由它自己从这句话推出来，然后直接开始办这件事。之后它自己改自己：用户纠正了语气、同类任务反复出现却没手册、职责和现实对不上，它用 `build` 改人设、工作方式、技能手册、外部连接，在后台进行。每次成长在对话里留一条记录。

**为什么**：用户知道要什么事，不知道要什么 prompt。配置是我们该替 bot 学会的能力，不是该交给用户的作业。

**在哪**：`crew-server/src/index.ts` `createBotFromBrief()`、`infer-bot.ts`；`extensions/build.ts`；`types.ts` 的 `GrowthEvent`；前端 `Thread.tsx` 的 `born` / `evolved` 通告。

## 6. 自主度三档，副作用只有一个口

**主流**：每个工具一个权限开关，或者每次动作弹确认。

**我们**：每个 bot 一个自主度：只告诉我 / 备好等我点 / 直接办。所有会改变外部世界的动作（付钱、下单、发消息、改别人日程）只走 `act`，由自主度和金额决定是直接执行还是先发确认卡；bot 不允许口头说「已办好」。外部系统里的写操作由 bot 自己判断可逆性，拿不准先问一句，绝不用写操作探测权限。

**为什么**：用户能理解的是「这个人我放多少权」，不是二十个工具各自的开关。把副作用收进一个口，审计、撤销、确认才有落点。

**在哪**：`types.ts` `Autonomy`；`identity.ts` `AUTONOMY_RULES`；`extensions/act.ts`。

## 7. 多个 bot 是同事，不是子任务

**主流**：orchestrator 拆子任务给 worker，用户看不见中间过程。

**我们**：bot 之间靠 @ 转达，像同事在 IM 里交接；多 bot 协作开一个群（一件事一个群，有牵头人和成员），协作过程就在群里可见，用户随时能插一句。每个 bot 在飞书、企业微信等 IM 里是一个独立机器人、一套凭据；把我们的 bot 拉进一个 IM 群，这边就镜像出一个群。

**为什么**：不透明的编排出了问题用户无从下手。同事关系用户天然会用：@谁、拉谁进群、看群里谁说了什么。

**在哪**：`bots.ts` 的 `crew:handoff` 事件与 `parseMentions`；`types.ts` `Matter`；`channels.ts` 的群镜像。

## 7b. 事项只由 bot 自己建，来源由运行时记

**主流**：把「谁被点名」当成「谁接了活」，系统替模型记账。

**我们**：只有 `todo` 工具能建事项，标题是模型写的一句人话。@ 只负责把话转达过去，是不是活由收到的人判断——被点名、道谢、同步进度都不建。牵头人正经派活走 `todo(create, assignee=某人, brief=交代清楚)`，事项直接建在对方名下，`brief` 是对方唯一看得到的交代（群里 bot 的发言默认不互相同步）。

事项归谁、属于哪个群、谁交办的、用户从哪个入口说的，全部由运行时按当前这一轮的来路快照进 `TodoOrigin`，模型指定不了也伪造不了。这样事项上才能长出「飞书」「@架构大师 交办」「例行」这类标签。

**为什么**：系统替模型记账，代价是把「回复里出现 @」等同于「交接了一件活」，看板会被点名和致谢淹没，标题只能靠正则从句子里截。宁可模型偶尔漏记一条——每轮系统提示词都会把它未完成的事项列给它看——也不要一块全是碎句的看板。

**在哪**：`crew-server/src/extensions/todo.ts`；`router.ts` 的 `crew:handoff`（转达不建事项，送不到就把那条事项标 blocked）；`types.ts` `TodoOrigin`。

## 8. IM 是同一段关系的不同入口

**主流**：每个渠道一个独立 bot 实例，或者渠道消息进入不同的会话。

**我们**：飞书、企业微信、Slack、Telegram 发来的消息和 App 里的私聊是同一条对话、同一段关系。消息正文开头带来源标记（【飞书】），回复自动送回来源；bot 说话方式不因来源变化，也不复述来源。

**为什么**：用户在哪个入口出现是他的事，bot 记住的应该是这个人。

**在哪**：`bots.ts` `sourceMark()`；`channels.ts` `deliver()`；`identity.ts` 「工作方式」第二条。

## 9. bot 有自己的电脑

**主流**：agent 在沙箱里跑，用户看不到；或者接管用户自己的浏览器。

**我们**：每个 bot 一台云桌面，用户在 App 里能看到它的屏幕直播。没有开关机概念：卡片常显屏幕，闲置 30 分钟休眠、留最后一帧，打开就唤醒。用户和 bot 可以同时在这台电脑上操作，没有「接管」。分辨率跟随窗口。

**为什么**：agent 用电脑干活时用户想看，而且经常要帮它一把（登录、验证码）。给它一台自己的电脑，比借用户的电脑或让它在黑箱里跑，都更像一个同事。

**在哪**：`crew-server/src/desktop.ts`（Xvnc + noVNC + Playwright）；`types.ts` `Desktop`；前端桌面卡片。

## 10. bot 住在常开的机器上，你的电脑只是路牌

**主流**：本地跑，关机就停；或者全托管云，用户的登录态搬不上去。

**我们**：一份数据（聊天、事项、记忆、技能、连接、密钥）随时可以整体搬到一台常开的机器上（自己的云主机，或托管云端），本机变成路牌，App 自动跟过去。本机的 agent 和登录态不搬，云上的 bot 借用本机的：本机关机它就借不到，接受这个代价，不把登录态搬上云、不烤进镜像。

**为什么**：bot 要能在你睡觉时干活，所以得住在常开的地方；但账号登录态属于用户的设备，不属于云。

**在哪**：`crew-server/src/machine.ts`、`runtime.ts`；`remote-install.ts`；`~/.crew/moved.json`；前端 `RuntimeView.tsx`。

## 11. 不换模型，把「看」外包

**主流**：发了图片就切多模态模型，或者整个产品用一个昂贵的多模态模型。

**我们**：主模型保持不变（便宜、快、无视觉）。图片、PDF、PPT、Word、Excel、扫描件统一进一个 `see(路径, 想知道什么)` 工具：能抽文字的抽文字，图和扫描件交给一个视觉模型描述，文件长又带问题时先压成答案。主模型自己有视觉时图片照旧直接进消息。

**为什么**：切模型等于换了这个 bot 的脑子：人设、工具习惯、成本全变，而且整轮对话都在贵模型上跑。外包一次调用就够。

**在哪**：`crew-server/src/vision.ts`、`extensions/see.ts`、`scripts/extract.py`、`office.ts`。

## 12. 模型和连接的密钥是产品的，不是用户的

**主流**：用户自己填 API key，选模型。

**我们**：模型、密钥、连接器凭据内置在产品里，用户界面上没有这些项；bot 要接外部服务时发授权卡，走 OAuth，不向用户要凭据。

**为什么**：填 key 的人不是我们的用户。

**在哪**：`~/.crew/config.json`（600 权限，只在服务端读）；`crew-server/README.md` 「配置」一节；`extensions/connect.ts`。

## 13. 版本就是提交，升级一下就到

**主流**：发版号、发布说明、用户手动更新。

**我们**：版本 = git 提交。App 里有更新就出一个标签，点一下升级；bot 住在远端机器时，远端一起升级（云机器用只读部署密钥自己拉代码，代码改动只重启，Dockerfile 变了才重建镜像）。

**在哪**：`crew-server/src/version.ts`、`upgrade.ts`；前端 `SettingsModal.tsx` 「关于」。

## 14. 界面说用户的语言，bot 说用户想听的语言

**我们**：设置里的语言是 App 的语言，十种，按浏览器语言自动选，日期时间跟着走。bot 说什么语言是另一项：默认跟界面，也可以「跟我说的」（用户换语言它就换）。

**在哪**：`bot-crew/src/i18n/`；`identity.ts` `LANGUAGE_RULE`。

## 15. 界面不解释自己

**我们**：没有劝导句、没有「你可以……」的说明小字，空状态两三个字；右侧只有一个面板，身份打开时借事项的位置，关掉就还回去；弹窗一律 portal 到 body。

**为什么**：解释性文案是设计没做完的标志，也是「AI 味」的主要来源。

**在哪**：`bot-crew/src/i18n/locales/zh.ts` 是文案的源头；`Thread.tsx` `PanelToggles`。

## 16. 一台电脑、一条池、密钥不经模型

**我们**：所有 bot 共用云机器上的一台电脑（一个桌面、一个 Chrome、一个登录态），每个 bot 一个 Playwright MCP 进程挂同一 CDP、各在自己的标签页里干活，不加锁；屏幕在每个工作区显示同一块。池只有一条：手册、外部工具（MCP 服务和走一键登录的平台是同一类，差别只在授权方式）、素材包；搜到不等于要装——read 手册用一次，用户纠正过或同类活反复来才 build(add) 长在身上。bot 接 IM 首选自己去平台后台建机器人，凭据用 `harvest` 由服务端从页面读进配置，模型只见遮罩后的回显；任何出站正文里出现已知密钥一律替换为 ••••。GUI 有两种手段：普通网页走 Playwright 文字快照（便宜、准、bot 自己一步步点），画板、设计器、剪辑、拖拽、桌面软件和长流程交给 `operate`——一个能看屏幕的模型（`guiModel`，留给 GPT-6 一类 computer-use 模型）看截图、出一个动作、执行、再看，云机器走共用电脑的 X 显示（xdotool），Mac 本地走 screencapture + cliclick，一套循环两边通用；整块屏幕一次只给一个 bot。

**为什么**：电脑是团队的工作站，登录一次全员可用；每 bot 一台既浪费内存也让登录态碎在各处。装上的东西每轮都占提示词，装配应当是有证据的进化而不是召回的副作用。端到端操作浏览器的 bot 没有理由再让用户抄一遍密钥，但模型不可信，所以读取和落盘都在它看不见的地方完成。

**不做**：不给 bot 通用桌面（截图点击）栈；不给每个 bot 单独的浏览器；不让密钥进对话、卡片字段或文件；不在出生时编手册。

**在哪**：`crew-server/src/desktop.ts`（共用电脑、`readPage`）；`gui.ts` + `extensions/operate.ts`（手）；`extensions/computer.ts`、`extensions/harvest.ts`、`secrets.ts`；`library.ts` / `extensions/library.ts`（看·用·进化）；`builtin-skills.ts` 的「IM 渠道接入」两条路线；`upgrade.ts` `waitIdle`（部署等 bot 空闲）。
